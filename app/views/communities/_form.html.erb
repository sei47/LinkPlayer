<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
        <% community.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= image_tag(community.image.url)if picture.image && picture.image.url %>
    <%= form.file_field :icon %>
  </div>

  <div class="field">
    <!---><% if action_name == 'edit' %>
      ゲーム選ぶ処理
    <% end %><--->
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
